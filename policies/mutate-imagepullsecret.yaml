apiVersion: kyverno.io/v1
kind: ClusterPolicy
metadata:
  name: mutate-imagepullsecret
spec:
  rules:
    - name: mutate-imagepullsecret
      match:
        resources:
          kinds:
          - Pod
      mutate:
        patchStrategicMerge:
          spec:
            imagePullSecrets:
            - name: dockerhub  ## imagePullSecret that you created with hb docker account
            (containers):
            - (image): "!mordor-nexus* | !registry* | !gitlab*" ## images not starting with your private registry